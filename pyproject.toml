[project]
name = "deskpilot"
authors = [
    { name = "mdad-elec", email = "61062853+mdad-elec@users.noreply.github.com"}
]
description = "In-Desk guided assistant for ERPNext — answers from live data, drives the screen, cites your SOPs."
# 3.10 is the floor for Frappe v15, the current stable line and what the
# marketplace lists against. Frappe's develop branch (v17) pins >=3.14 — do not
# copy that floor here, it would make this app uninstallable on every stable site.
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = [
    # "frappe~=16.0.0" # Installed and managed by bench.
]

# Frappe Cloud refuses an app that does not declare this, and bench warns on
# get-app. One branch serves both supported lines, so the range spans them rather
# than pinning one — tests/test_packaging.py checks it against the CI matrix, so
# widening the claim without testing it fails the build.
#
# Only frappe is listed. The app installs on a plain Frappe bench with no ERPNext
# (that is what CI installs), and naming erpnext here would narrow it for no reason.
[tool.bench.frappe-dependencies]
frappe = ">=15.0.0,<17.0.0"

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

# These dependencies are only installed when developer mode is enabled
[tool.bench.dev-dependencies]
# package_name = "~=1.1.0"

# These apt dependencies will be installed from Ubuntu repositories when you host your app on Frappe Cloud
[deploy.dependencies.apt]
packages = []

[tool.ruff]
line-length = 110
target-version = "py310"

[tool.ruff.lint]
select = [
    "F",
    "E",
    "W",
    "I",
    "UP",
    "B",
    "RUF",
]
ignore = [
    "B017", # assertRaises(Exception) - should be more specific
    "B018", # useless expression, not assigned to anything
    "B023", # function doesn't bind loop variable - will have last iteration's value
    "B904", # raise inside except without from
    "E101", # indentation contains mixed spaces and tabs
    "E402", # module level import not at top of file
    "E501", # line too long
    "E741", # ambiguous variable name
    "F401", # "unused" imports
    "F403", # can't detect undefined names from * import
    "F405", # can't detect undefined names from * import
    "F722", # syntax error in forward type annotation
    "W191", # indentation contains tabs
    "UP030", # Use implicit references for positional format fields (translations)
    "UP031", # Use format specifiers instead of percent format
    "UP032", # Use f-string instead of `format` call (translations)
    "UP037", # quoted annotations
    "UP040", # Use type aliases instead of type annotations
    # Typography is deliberate: ∪ for set union in the retrieval docs, en dash and
    # minus sign in user-facing ERP field descriptions. These are not typos.
    "RUF001", # ambiguous unicode in a string
    "RUF002", # ambiguous unicode in a docstring
    "RUF003", # ambiguous unicode in a comment
]
typing-modules = ["frappe.types.DF"]

[tool.ruff.format]
quote-style = "double"
indent-style = "tab"
docstring-code-format = true
