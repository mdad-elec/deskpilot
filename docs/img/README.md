# Screenshots

Empty on purpose. The previous screenshots were taken on a live instance and showed
the old product name, a customised naming series, and business-specific fields — they
could not be published.

Retake these after installing on a clean site, with the assistant named Deskpilot, on
stock ERPNext demo data. The Frappe Marketplace listing needs them too.

Worth capturing:

| File | What it should show |
|---|---|
| `walkthrough.png` | A multi-step guided walkthrough on a Sales Order, spotlight visible |
| `grid-column.png` | A highlight landing on a column heading inside a child table |
| `add-row.png` | "Add Row" resolved to the grid's own control, not a field |
| `sessions.png` | The conversation list |
| `ghost-fab.png` | The dismissed launcher |
